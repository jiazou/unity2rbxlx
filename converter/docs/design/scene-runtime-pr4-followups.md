# PR4 follow-ups (deferred)

PR4 landed:

- Host runtime (`converter/runtime/scene_runtime.luau`) with the full
  Piece 6 service surface (component registry, GetComponent built-in
  fallback, `instantiatePrefab`, `addComponent`, global lookups,
  lifecycle-scoped scheduler, `host.connect` event wiring,
  `host.destroy` DFS-deepest-first, cross-domain v1 nil-injection +
  log).
- Thin entrypoints generated by `converter/autogen.py`:
  `SceneRuntimeClient` (LocalScript in StarterPlayerScripts) and
  `SceneRuntimeServer` (Script in ServerScriptService).
- `SceneRuntimePlan` ModuleScript embedded in ReplicatedStorage, built
  from the planner artifact at `_subphase_inject_scene_runtime`.
- Conversion-time cross-domain edge report:
  `compute_cross_domain_edges` enumerates client<->server peer
  references at conversion time and appends them to `UNCONVERTED.md`
  via `render_cross_domain_report`.
- `_enforce_scene_runtime_legacy_until_pr4` lifted in `u2r.py` and
  `convert_interactive.py`. `generic` flows through; `auto` is still
  rejected (PR5 turf).
- Codex P3 from PR3b followups: `SceneRuntimeDisplacedInstance` now
  carries `owner_kind` + `owner_ref` in addition to the legacy
  `scene` field (kept for one release).
- Test matrix: 13 Luau-harness behavioral tests for the host runtime
  via standalone `luau`, 19 plan-encoder/edge-report tests, 9
  pipeline-emit integration tests.

The carve-outs below are deferred from PR4. They are NOT merge-
blocking for PR4's host-runtime contract: a `generic` conversion
without them produces a working host runtime with two known limitations
(documented below).

## 1. `scene_converter` inactive retention (generic-only)

**Where:** `converter/converter/scene_converter.py` -- `_convert_node`
prunes inactive GameObjects unconditionally; `_apply_prefab_overrides`
and the prefab-instance modifications path also drop nodes when
`m_IsActive == 0`.

**Why deferred:** PR4's primary scope is the host runtime + Piece 6
services. The inactive-retention carve-out is generic-only and
requires plumbing `scene_runtime` (the planner artifact) through
`convert_scene`'s parameter list and into `SceneConversionContext` so
the recursive `_convert_node` can check "is this inactive node
runtime-referenced?". The plumbing crosses
`scene_converter` <-> `prefab_packages` <-> `pipeline` boundaries and
touches code that affects legacy output byte-for-byte. Best-effort
estimate: another ~250 LOC + a fresh round of byte-equivalence guards
on the legacy path.

**Behavioural impact today (without this carve-out):** A runtime-bearing
MonoBehaviour attached to an inactive GameObject is pruned from the
converted scene. The host runtime's plan still lists the instance, so
`workspaceFind` returns `nil` and the `Awake` injection-time
`gameObject` binding is `nil`. The module fails to construct; the
warning logged ("[scene_runtime] unknown ... skipping instance") is
the operator's signal.

**Test fixture (when implementing):** A scene with one inactive
GameObject carrying a runtime-bearing MonoBehaviour AND referenced by
another (active) GameObject's serialized field. Under
`--scene-runtime=generic` the dormant object appears in the converted
place stamped with `_SceneRuntimeId`. Under `--scene-runtime=legacy`
the place is byte-unchanged.

## 2. `ui_translator` asset/prefab serialized-field child suppression
(generic-only)

**Where:** `converter/converter/ui_translator.py` -- UI conversion
emit path; specifically where serialized-field references to assets
or prefabs propagate child elements into the converted UI tree.

**Why deferred:** Same reason as item 1 -- generic-only carve-out
that requires plumbing `scene_runtime` into the UI conversion paths.
The UI translator also has the most fragile byte-equivalence
guarantees in the converter (sprites/atlas slicing); changes need a
larger byte-equivalence test suite.

**Behavioural impact today (without this carve-out):** A converted
Canvas with a runtime-bearing controller whose serialized-field
points at a prefab gets BOTH a static prefab descendant (emitted by
the legacy serialized-field walker) AND a runtime-instantiated copy
(when the controller calls `host.instantiatePrefab(...)`). The
duplicated tree visually appears as two copies of the same UI
element; the operator will spot it via QA but the converter doesn't
flag it.

**Test fixture (when implementing):** A scene with a Canvas + a
runtime-bearing UI controller whose serialized field points at a
prefab. Under generic the Canvas in the converted place has no
static prefab descendants for that field. Under legacy the static
descendants persist.

## 3. Disable `ClientBootstrap` + legacy autogen under generic

**Where:** `converter/converter/pipeline.py::_subphase_inject_autogen_scripts`
emits `ClientBootstrap`, `GameServerManager`,
`CollisionFidelityRecook`, `CameraRigFollower`, and the FPS
scaffolding regardless of `scene_runtime_mode`. The design doc PR3a
row says "all legacy repair passes off, incl. `write_output` emit-time
subphases" under generic.

**Why deferred:** PR4 chose coexistence: the host runtime injects ONLY
runtime-bearing MonoBehaviours, so the legacy `ClientBootstrap`'s
side-effect-module require loop doesn't conflict with the host
runtime's lifecycle wiring. They target disjoint script sets.
Disabling the legacy emit cleanly requires deciding which autogen
scripts have generic equivalents (none today have generic equivalents
for collision groups / fidelity / camera rig) and which are
legitimately game-agnostic infrastructure.

**Behavioural impact today (without this disable):** The converted
place ships with both `ClientBootstrap` (legacy) and
`SceneRuntimeClient` (generic). They don't fight at runtime because
the host runtime targets `runtime_bearing` modules and
`ClientBootstrap` targets side-effect modules. The eventual cleanup
should remove `ClientBootstrap` under generic and replace
`CameraRigFollower` etc. with host-runtime-aware equivalents.

## 4. Plan-sharding loader

**Where:** `converter/converter/autogen.py::_plan_to_luau` emits the
plan as a single ReplicatedStorage ModuleScript. Roblox's per-script
source size budget caps how large the plan can be before publish
fails.

**Why deferred:** The design doc explicitly lists "Plan-sharding
loader if needed" -- "if needed" is the operative phrase. PR4 doesn't
yet have a concrete project that overflows the budget; the trash-dash
and SimpleFPS canaries (PR5 scope) are the natural place to measure.

**Behavioural impact today:** Projects whose plan ModuleScript
exceeds Roblox's source budget will fail at publish. The error
surfaces in the upload phase, which is recoverable.

## 5. Codex P2/P3 absorptions

(Filled in once codex review runs against PR4.)

## Carry-over markers

- [ ] PR4-followup: implement `scene_converter` inactive retention.
- [ ] PR4-followup: implement `ui_translator` serialized-field
      child suppression.
- [ ] PR4-followup: scope a "generic emit-side off-switch" PR that
      decides per-autogen-script whether to keep or replace under
      generic.
- [ ] PR4-followup: measure plan size on PR5's canary projects;
      implement sharding if any place overflows the publish budget.
- [ ] PR5: remove the legacy `scene` alias on
      `SceneRuntimeDisplacedInstance` after one release cycle.
